import React from 'react';

export default async function LeaderboardItem({data}) {


    return (<>
        <span>${data.author}</span>
        <span>${data.author}</span>
        <span>${data.author}</span>
    </>);
}